<div class="jumbotron jumbotron-fluid">
    <div class="container">
        <h1>{{ page.title }}</h1>
    </div>
</div>

<div class="rte">
    <div class="container">
        {{ page.content }}
    </div>
</div>
